# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/orderhub?schema=public"

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-strong-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# Public base URL used to build invoice links + QR codes
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Seed credentials for the initial Super Admin
SEED_ADMIN_EMAIL="admin@orderhub.com"
SEED_ADMIN_PASSWORD="Admin@12345"

# Invoice email delivery (SMTP). When configured, every new order — from the
# storefront or the dashboard — emails its invoice PDF automatically. Leave
# SMTP_* unset to disable email (orders still succeed).
# For Gmail, use an App Password (not your normal password):
#   https://support.google.com/accounts/answer/185833
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-gmail-address@gmail.com"
SMTP_PASS="your-16-char-app-password"
# From header; defaults to SMTP_USER if unset.
SMTP_FROM="your-gmail-address@gmail.com"
# Where invoices are sent. Defaults to Malyaseri9@gmail.com.
INVOICE_NOTIFY_EMAIL="Malyaseri9@gmail.com"
